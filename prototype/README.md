# Prototype

**Status:** Decided (V4.1) · Part of the Wingman product & engineering spec.

Open `index.html` directly — no build, no backend. Controls (top): EN/FR, Reduce motion, Offline. Try: activate the
Radar, tap a mood dot, Send a Signal (diffuse wave), run the selfie exchange (calm timer + "Let it expire"), reach
*Connection confirmed* (violet→rose halo fusion), open Mission Meet (type a phone number or `@handle` to trigger the
anti-contact filter), confirm the outcome, land in Cooldown. Settings has a hidden Admin moderation preview link.
Files: `index.html`, `styles.css`, `app.js`. All data is fictional.
