# Sprint 2

**Status:** Decided (V4.1) · Part of the Wingman product & engineering spec.

Signal (quota, one-active-per-pair, silent 10-min expiry), accept → Connection + ActiveUserLock (atomic), selfie
exchange with opaque media + presigned upload + short signed view, notifications, and the connection state machine to
mutual validation. Exit: full Radar→confirmed path works with server-authoritative timers.
