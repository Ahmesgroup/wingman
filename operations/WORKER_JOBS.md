# Worker Jobs

**Status:** Decided (V4.1) · Part of the Wingman product & engineering spec.

Expiration/timeout consumers (signal, selfie, mission, ticket, cooldown), retention purges over `purgeAt`, media
deletion + orphan reconciliation, push delivery, usage-counter resets, Radar reaper, moderation packaging. All
idempotent and safe alongside user actions.
